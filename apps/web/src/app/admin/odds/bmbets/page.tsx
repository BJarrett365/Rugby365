import { BmbetsScrapeClient } from "./BmbetsScrapeClient";

export const metadata = {
  title: "BMbets odds · Rugby365 CMS",
};

export default function AdminBmbetsOddsPage() {
  return <BmbetsScrapeClient />;
}
