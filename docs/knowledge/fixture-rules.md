# Fixture Rules

Every approved fixture requires: Competition, Season, Home, Away, Kick-off, Status, Primary provider, Provider mapping (when sourced).

No season → not approved. Use `SEASON_UNMAPPED`, hold for review, exclude from normal stats. Audit every season repair.
